
<div class='add_categories'>
<% if @category.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@category.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
      <% @category.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: [@current_user, @category ] do |form| %>
  <p>
    <%= form.label :project_title %><br>
    <%= form.text_field :project_title %>
  </p>
  <p>
    <%= form.label :details %><br>
    <%= form.text_field :details %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
</div>